# config.py
# This file includes includes major config parameters to keep the same values across different modules

# Manual Radio values
stick_input_range = [-100, 100]

# PWM outputs range
PWM_output_range = [1100, 2000]
