# config.py
# This file includes includes major config parameters to keep the same values across different modules

# Manual Radio values
radio_throttle_inputs = [-100, 100]
radio_roll_inputs = [-10, 10]
radio_pitch_inputs = [-10, 10]
radio_yaw_inputs = [-10, 10]

# PWM outputs range
PWM_output = [1100, 2000]
